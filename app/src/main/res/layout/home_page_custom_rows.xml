<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:contentPadding="10dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp"
    app:cardUseCompatPadding="true"
    app:cardBackgroundColor="@color/custom_blue">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/catData"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="@string/event_details"
            android:fontFamily="sans-serif"
            android:textColor="@color/custom_white_background"
            android:layout_weight="1"
            android:textSize="20sp"/>

        <ImageButton
            android:id="@+id/catDelBtn"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@drawable/ic_baseline_attach_file_attachment_white"
            android:background="@drawable/pdf_add_shape"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>